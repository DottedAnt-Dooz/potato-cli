{
  "schemaVersion": 2,
  "rules": [
    "Commands affecting one desktop must run sequentially, including screenshots taken after actions.",
    "ok reports command execution; assert data.exists, data.conditionMet or an application postcondition separately.",
    "verified is null when verification was not requested; successful input delivery does not prove the expected result.",
    "Selectors are case-insensitive literal/wildcard matches by default. -Regex accepts regular expressions, not wildcard syntax.",
    "TimeoutMs bounds retries; an individual UI Automation provider call may exceed it.",
    "Use -Flag=value for values starting with a dash. Negative numeric values also work as separate tokens.",
    "VisibleControls is the default and forbids hotkey (including dialog Enter) and Shortcut clearing. AllowShortcuts requires user/testcase authorization and per-action FallbackReason/FallbackEvidence; clipboard is unsupported.",
    "Commands use a bounded desktop mutex. DesktopBusy means not dispatched; outcome unknown requires observing the postcondition before retry.",
    "Invalid bounds are returned as null with boundsStatus/propertyErrors; do not infer missing UIA support from missing geometry."
  ],
  "selectorOptions": [
    "Name",
    "AutomationId",
    "ClassName",
    "Class",
    "ControlType",
    "ProcessName",
    "WindowTitle",
    "Regex",
    "Recurse",
    "FindFirst",
    "TimeoutMs",
    "PathJson",
    "SelectorJson",
    "ProcessId",
    "ModalOnly"
  ],
  "commands": {
    "help": {
      "usage": "help [-Topic type]",
      "notes": "Read command guidance without a desktop session or state mutation."
    },
    "state": {
      "usage": "state [-Clear]",
      "notes": "Clear resets CLI context, not application state."
    },
    "start": {
      "usage": "start -ProcessName app.exe [-Arguments='quoted process arguments'] [-WaitForWindowMs 15000] [-Maximize] [-KillExisting] [-RequireNewProcess] [-WaitForPreviousExitMs 3000]",
      "notes": "FilePath/Path and positional launch target are also accepted. KillExisting is destructive and must only be used for disposable test-owned apps. Check data.windowFound. A file-association launch does not exercise an application's Open UI. Only executable launches are accepted. RequireNewProcess waits briefly for a closing prior instance, rejects a still-running app, and returns ownedProcessId for scoped cleanup."
    },
    "focus": {
      "usage": "focus -ProcessName app [-WindowTitle '*title*'] [-ProcessId 1234] [-Maximize] [-TimeoutMs 5000]"
    },
    "windows": {
      "usage": "windows [-ProcessName app] [-WindowTitle '*title*'] [-TimeoutMs 0]",
      "notes": "With ProcessId, includes modal Window descendants exposed beneath the owner in UIA; data.windows includes isModal. Close owned dialogs before parent windows."
    },
    "observe": {
      "usage": "observe [-Depth 2] [-MaxElements 120]",
      "notes": "Use for unknown UI states. Prefer targeted select/read for known postconditions."
    },
    "select": {
      "usage": "select <selector options> [-MaxResults 20]",
      "notes": "Queries elements; it does not select a dropdown item. data.count=0 is a valid query result. Open a dropdown before looking for its items. Discover role before imposing a guessed ControlType; use ProcessId and ModalOnly for scoped dialog queries."
    },
    "click": {
      "usage": "click <selector options> [-Method Auto|Mouse|Invoke] [-Center true] [-Button Left|Right] [-Focus false] [-ElementFocus false] [-OffsetX 0] [-OffsetY 0] [-VerifyDisappeared]",
      "notes": "Use Auto unless a specific actuation method is required. Auto tries UIA patterns then a visible mouse click; Mouse uses live geometry. Invoke requires InvokePattern and reports supported patterns when unavailable. Methods are explicit; do not retry a possibly completed action without observing its result."
    },
    "click-coordinate": {
      "usage": "click-coordinate -X 100 -Y 100 [-Button Left|Right]",
      "notes": "Fallback only; retain a screenshot and explain the missing selector route."
    },
    "type": {
      "usage": "type -Text 'literal text' [<selector options>] [-FocusMethod Auto|UIA|Mouse] [-PreDelete] [-ClearMethod Selection|Shortcut] [-Verify] [-VerifyMode Exact|Contains|NormalizedExact|NormalizedContains] [-VerifyTimeoutMs 3000] [-MaxAttempts 0] [-TypeByCharacter] [-Focus]",
      "notes": "Sends literal Unicode keyboard events; no clipboard. With an explicit selector, Auto tries UIA focus then a visible mouse click if focus is unconfirmed; Mouse requests visible click focus directly. The target must be enabled, writable, and confirmed focused before any text is sent. A separate click is usually unnecessary. -Verify polls UIA readback up to VerifyTimeoutMs without resending input; MaxAttempts 0 uses the deadline. Normalized modes reconcile line endings. UseWildcardForVerify remains an alias for literal Contains. Failure returns verification attempts, observed length, and read error without exposing field contents. -TimeoutMs applies to selector lookup, not readback. PreDelete uses TextPattern selection plus Backspace by default; Shortcut clearing requires explicit allowance."
    },
    "hotkey": {
      "usage": "hotkey -Keys ^s -InteractionPolicy AllowShortcuts -FallbackReason <reason> -FallbackEvidence <reference>",
      "notes": "Disabled by default, including Enter confirmations. One chord only, no grouped sequences or clipboard shortcuts. Requires user/testcase permission."
    },
    "drag": {
      "usage": "drag -StartX 100 -StartY 100 -EndX 200 -EndY 200 [-Smooth]",
      "notes": "Absolute coordinates; record fallback rationale and evidence."
    },
    "hover": {
      "usage": "hover <selector options> | hover -X 100 -Y 100"
    },
    "wait-element": {
      "usage": "wait-element <selector options>",
      "notes": "Assert data.exists, not just ok. Use the selector's TimeoutMs. Window queries include the working window itself and other process windows; a launched window does not prove the application's next control is ready."
    },
    "wait-file": {
      "usage": "wait-file -Path 'output.ext' [-TimeoutMs 10000] [-MinBytes 1] [-StableMs 500] [-WaitForNotExists]",
      "notes": "Assert data.conditionMet. StableMs requires unchanged length AND last-write time. Directories are not files. Stability alone does not prove file validity or content. Use unique execution paths to avoid stale-file passes."
    },
    "read": {
      "usage": "read <selector options>",
      "notes": "Returns data.text from ValuePattern, TextPattern, or element name. Choose a meaningful expected-result assertion for the target."
    },
    "screenshot": {
      "usage": "screenshot [-OutFile 'evidence.png'] [<selector options>] [-X 0 -Y 0 -Width 800 -Height 600]",
      "notes": "Evidence capture alone is not an assertion that the image is correct."
    },
    "close-window": {
      "usage": "close-window [-ProcessName app] [-WindowTitle '*title*'] [-TimeoutMs 0]",
      "notes": "With no selector, targets only the working window. A ProcessId query includes nested modal windows and closes them before the parent. An explicit selector miss never closes another working window. data.closed counts accepted close requests; inspect remaining windows for cleanup verification."
    },
    "report": {
      "usage": "report -Step 1 -Status PASS -Description 'Observed result' [-Screenshot]",
      "notes": "Records a report event; it does not validate that the status is justified."
    }
  },
  "globalOptions": {
    "InteractionPolicy": "VisibleControls (default) or AllowShortcuts",
    "FallbackReason": "Required explanation for every authorized shortcut",
    "FallbackEvidence": "Required observation/screenshot reference for every authorized shortcut",
    "LeaseTimeoutMs": "Desktop lock acquisition bound, 0..60000; default 15000"
  }
}
